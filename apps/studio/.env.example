# Preview Secret Configuration
# This must match the SANITY_PREVIEW_SECRET in your Netlify environment variables

# For development (local)
SANITY_STUDIO_PREVIEW_SECRET=sanity-preview-secret
SANITY_STUDIO_PRODUCTION_URL=http://localhost:3001

# For production (deployed Studio)
# Update these in your Vercel environment variables:
# SANITY_STUDIO_PREVIEW_SECRET=[same secret as Netlify SANITY_PREVIEW_SECRET]
# SANITY_STUDIO_PRODUCTION_URL=https://digiprint-main-web.netlify.app
